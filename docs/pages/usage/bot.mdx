# Bot

> Lichess API Docs - [Bot](https://lichess.org/api#tag/Bot)

### Get online bots

```ts
client.bot.online(nb?: number)
```

### Upgrade to Bot account

```ts
client.bot.UPGRADE()
```

### Stream Bot game state

```ts
client.bot.stream(gameId: string)
```

### Make a Bot move

```ts
client.bot.move({ gameId: string, move: string })
```

### Write in the chat

```ts
client.bot.chat({
  gameId: string,
  room: 'player' | 'spectator',
  text: string,
})
```

### Fetch the game chat

```ts
client.bot.chat.fetch({ gameId: string })
```

### Abort a game

```ts
client.bot.abort(gameId: string)
```

### Resign a game

```ts
client.bot.resign(gameId: string)
```
